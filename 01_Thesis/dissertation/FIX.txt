The main risky points I would check first

If you hand me the thesis, I would immediately test whether these are fully defined:

exact wildfire state update equations
exact observation model
exact feature extraction formulas
exact fuzzy membership functions
exact concept construction rules
exact local decision rule base
exact j_DSS aggregation logic
exact A_DSS global coordination logic
exact scenario and benchmark definitions for Chapter 7

If any of these are only described conceptually, then I can still code them, but not as a purely literal implementation.

1. UI / dashboard

dashboard / interface description
Chapter 7 evaluation structure
any function whose exact rule, weighting, or aggregation form is referenced but not numerically fixed in the final text