# Core engine
numpy>=1.23

# Dashboard. The wide analysis tables open in a modal window via st.dialog,
# which arrived in 1.35.
#
# THE FLOOR IS 1.49 BECAUSE OF width, NOT BECAUSE OF A FEATURE. Streamlit
# deprecated use_container_width with a removal date that has now passed,
# so the app sizes its elements with width="stretch" instead. On builds
# before 1.49 the same argument means a pixel count, and a string there
# is not a deprecation warning but a broken layout, so the floor has to
# move with the call sites.
streamlit>=1.49
# used by the dashboard tables and the Layer 4 analysis charts; it was an
# undeclared dependency that happened to arrive with streamlit
pandas>=1.5
matplotlib>=3.6
plotly>=5.0
pillow>=9.0
# Word (.docx) export of the validation report
python-docx>=0.8.11
# Excel (.xlsx) export of the step analysis, the agent view and the decision
# log. TSV lost every number to text and arrived in one column unless the
# reader ran the import wizard.
openpyxl>=3.1
# Generative-AI stage 3 runs on the user's Claude subscription via the Claude
# Code CLI (`claude -p`); install Claude Code separately, no Python package.
# mouse-driven map editing (paint forests, drop assets and ignitions)
streamlit-drawable-canvas>=0.9.3

# Tests
pytest>=7.0

# Optional: GIS raster import (DEM / fuel rasters)
# rasterio>=1.3

# Optional: human readable scenario files
# pyyaml>=6.0
rasterio>=1.3
requests>=2.28
